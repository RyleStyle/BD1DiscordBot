module.exports = {
    name: "setdev",
    description: "HIDDEN",
    category: "dev",
    run: async (client, message, args) => {

        /*
        const fs = require("fs")
        client.config = require("../.././config.json")
        const devID = client.config.developerID;
        const token = client.config.token;
        const developer = args[0]


        if (message.author.id !== devID) return;


        client.config = {
            "token": token,
            "developer": developer,
            "developerID": devID
        }
        fs.writeFile("./config.json", JSON.stringify(client.config, null, 4), (err) => {
            if (err) throw err;
        });

        await message.react('✅')*/
    }
}